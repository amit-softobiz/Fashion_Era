export interface Slide {
  img: string;
  title: string;
}

export interface BannerItem {
  img: string;
  title: string;
}

export interface Product {
  name: string;
  image: string;
  price: number;
}
